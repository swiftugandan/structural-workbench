# M03-F physical / analytical hierarchy UI

Bounded presentation of split lineage already stored on members (`parentMemberId`, `stationRange`):

| Check | Result |
| --- | --- |
| `tests/hierarchy.test.mjs` | grouping + nav HTML |
| Browser `tests/e2e/hierarchy.spec.js` | split → tree groups → inspector lineage → analyse |

Does **not** claim automatic remeshing, analyse-time child UI, or parent M03 packaging.
