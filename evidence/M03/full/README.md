# M03 parent acceptance

Same-build packaging of accepted M03 slices A–F. Produce with:

```bash
npm run build
npm run verify:m03
```

Verifier: `npm run verify:milestone -- M03` (requires `evidence/M03/full` records from the runner).
