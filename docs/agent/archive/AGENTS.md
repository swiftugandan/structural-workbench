# Project agent instructions

Read and follow Agent.md in this directory before implementing this project. It extends the structural workbench specification with execution, verification, checkpoint and release rules.

Resolve the specification package as docs/spec when present, otherwise the accompanying handoff directory containing SPECIFICATION.md. Preserve higher-priority runtime instructions and applicable existing repository rules.

When installing this loader into a repository that already has AGENTS.md, merge this reference into the existing file instead of replacing its instructions.
