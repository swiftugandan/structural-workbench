# Initial implementation
The source-controlled handoff at 3017786 is the immutable baseline. Implementation
stays on the existing trunk, per project instruction. No framework or backend.
Use pure Rust sprs-ldl behind a solver trait. Use serde schema-shaped types,
semantic validation in Rust and compiled Draft 2020-12 Ajv for browser imports.
No runtime CDN. Reports and projects are standalone downloads.
M00 is not accepted until all required evidence exists. Later code profiles
remain disabled until resources and complete clause-level verification exist.
