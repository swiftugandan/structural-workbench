Locked your three picks.

## Decided
- **A** — keep the runtime spine
- **verified MVP** — evidence over demo polish
- **next** — M02 loads/combinations

## Board update
Replaced the open questions with an M02 execution spine: contracts → Rust combo → oracle/WASM fixtures → thin UI → hash-bound e2e. Platform/CUA blockers stay recorded, not waived.

One remaining choice on the board: plan-only vs kernel-first vs full vertical slice. Default I’d run is kernel-first.
